---
- name: "Get htpasswd for {{ user.name }}"
  ansible.builtin.shell: |
     podman run --rm --network=none ghcr.io/crc-org/crc-cloud/httpd-tools {{ user.name }} {{ user.password }} >> htpasswd.txt
