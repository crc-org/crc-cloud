---
#  https://github.com/crc-org/crc-cloud/blob/main/pkg/bundle/setup/clustersetup.sh#L229
- name: Ensure htpasswd.txt does not exists
  ansible.builtin.file:
    path: htpasswd.txt
    state: absent

- name: Create temporary directory
  ansible.builtin.tempfile:
    state: directory
  register: _temp_dir

- name: Get htpasswd
  ansible.builtin.include_tasks: get_htpasswd.yaml
  loop: "{{ users }}"
  loop_control:
    loop_var: user
  no_log: true

- name: Cleanup htpasswd.txt file
  ansible.builtin.shell: |
    sed -i '/^\s*$/d' htpasswd.txt

- name: Create secret with generic htpass-secret
  ansible.builtin.shell: >
    oc create secret generic htpass-secret
    --from-file=htpasswd=htpasswd.txt
    -n openshift-config
    --dry-run=client
    -o yaml > /tmp/htpass-secret.yaml
  register: _generic_secret
  retries: "{{ max_retries }}"
  delay: "{{ retry_delay }}"
  until: _generic_secret is succeeded
  changed_when: false

- name: Replace htpass-secret
  ansible.builtin.command: oc replace -f /tmp/htpass-secret.yaml
  register: _replace_htpass
  retries: "{{ max_retries }}"
  delay: "{{ retry_delay }}"
  until: _replace_htpass is succeeded
  changed_when: false

- name: Remove temporary directory
  ansible.builtin.file:
    path: "{{ _temp_dir.path }}"
    state: absent
